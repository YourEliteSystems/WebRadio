name: Build Linux (AppImage + Arch)

on:
  push:
    branches:
      - main
      - develop
      - feature/**
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:

permissions:
  contents: read

jobs:
  appimage:
    name: Build Linux AppImage (x86_64)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Node.js einrichten
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Dependencies installieren
        run: npm ci

      - name: Renderer bauen
        run: npm run build

      - name: Linux AppImage bauen
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx electron-builder --linux AppImage --x64 --publish=never

      - name: Build-Artefakte hochladen
        uses: actions/upload-artifact@v4
        with:
          name: webradio-linux-appimage
          path: |
            dist/*.AppImage
            dist/latest*.yml
            dist/latest*.json
            dist/*.blockmap
          if-no-files-found: error

  arch-package:
    name: Build Arch Linux Package (.pkg.tar.zst)
    needs: appimage
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      # Wir bauen das Arch-Paket in einem offiziellen Arch-Linux-Container.
      # Der WebRadio-Build selbst (AppImage) wurde bereits im vorherigen
      # Job erzeugt und wird hier heruntergeladen und eingebettet.
      - name: AppImage herunterladen
        uses: actions/download-artifact@v4
        with:
          name: webradio-linux-appimage
          path: dist

      - name: PKGBUILD und Quellen vorbereiten
        run: |
          set -euo pipefail
          mkdir -p dist/arch-build
          cd dist/arch-build

          # AppImage mit normalisiertem Namen ablegen.
          APPIMAGE_SRC="$(ls ../*.AppImage | head -n1)"
          # SemVer aus package.json (z.B. "1.0.6-beta.2")
          SEMVER="$(node -p "require('../../package.json').version")"
          # Arch pkgver darf KEINE Bindestriche enthalten → ersetze durch Punkte
          PKGVER_ARCH="${SEMVER//-/.}"

          cp "$APPIMAGE_SRC" "./webradio-${SEMVER}.AppImage"
          chmod +x "./webradio-${SEMVER}.AppImage"

          cp ../../assets/webradio.desktop .
          cp ../../assets/icons/tray.png .

      - name: PKGBUILD aus Template rendern
        run: |
          set -euo pipefail
          cd dist/arch-build
          SEMVER="$(node -p "require('../../package.json').version")"
          PKGVER_ARCH="${SEMVER//-/.}"
          APP_SHA="$(sha256sum "webradio-${SEMVER}.AppImage" | awk '{print $1}')"
          DESK_SHA="$(sha256sum webradio.desktop | awk '{print $1}')"
          ICON_SHA="$(sha256sum tray.png | awk '{print $1}')"
          # __PKGVER__ = Arch-Version (ohne Bindestriche)
          # __SEMVER__  = Original SemVer für Dateinamen und pkgdesc
          sed \
            -e "s|__PKGVER__|${PKGVER_ARCH}|g" \
            -e "s|__SEMVER__|${SEMVER}|g" \
            -e "s|__APPIMAGE_SHA256__|${APP_SHA}|g" \
            -e "s|__DESKTOP_SHA256__|${DESK_SHA}|g" \
            -e "s|__ICON_SHA256__|${ICON_SHA}|g" \
            ../../packaging/arch/PKGBUILD \
            > PKGBUILD
          echo "--- PKGBUILD (head) ---"
          head -n 45 PKGBUILD

      - name: Arch-Paket im Container bauen
        run: |
          docker run --rm \
            -v "$PWD/dist/arch-build:/work" \
            -w /work \
            archlinux:latest \
            bash -lc "
              set -euo pipefail
              pacman -Sy --noconfirm base-devel fakeroot squashfs-tools unzip
              useradd -m builder
              chown -R builder:builder /work
              sudo -u builder bash -c 'cd /work && SRCDEST=/work makepkg -s --noconfirm'
            "

      - name: Ergebnis-Artefakt hochladen
        uses: actions/upload-artifact@v4
        with:
          name: webradio-linux-arch
          path: dist/arch-build/*.pkg.tar.zst
          if-no-files-found: error
